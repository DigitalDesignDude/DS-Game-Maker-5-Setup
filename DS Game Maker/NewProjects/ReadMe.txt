This folder is where new DS Game Maker project files (.dsgm) are created by default.

Sometimes you may have to manually delete the cached .dsgm files that are stored here if they did not already delete themselves for whatever reason.

